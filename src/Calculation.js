class Calculation {
  sum(a,b) {
    console.log(a + b);
  }

  substraction(a,b) {
    console.log(a - b);
  }

  multiplication(a,b) {
    console.log(a * b);
  }

  division(a,b) {
    console.log(a / b);
  }
}

export default Calculation;
